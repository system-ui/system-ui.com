import { ThemeProvider, Styled } from 'theme-ui'

export default props =>
  <ThemeProvider
    theme={{
      styles: {
        root: {
          py: 6,
          color: 'white',
          bg: 'black',
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'center',
        },
      },
    }}>
    <Styled.root>
      {props.children}
    </Styled.root>
  </ThemeProvider>

![logo](https://contrast.now.sh/000/fff?size=128&fontSize=2&text=UI)

# System UI

